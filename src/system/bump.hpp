

#include <stdlib.h>

entt::entity Bump(int[] dir, int[] Mover_position, int[] MovedInto_position) {
    // remember to add in an extra clause for the component
  if(!E_registry.valid(entity)){
    printf("Non valid entity/Component doesn't exist!")
  } else if ( MovedInto_position[1] = dir[1] + position[1] && MovedInto_position[2] = dir[2] + position[2] ) {
    // need a way to check an entity against an already set-up position
    if(){
    }
  }
}
