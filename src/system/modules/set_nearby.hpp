// // currently shelving this for now
// #ifndef SET_NEARBY_INCLUDES
// #define SET_NEARBY_INCLUDES

// #include <iostream>

// #include "../../ECS_init.hpp"
// #include "../../global_entity_values.hpp

// #endif

// #ifndef SET_NEARBY
// #define SET_NEARBY

// void set_nearby(entt::registry &reg, entt::entity entity) {
//   for (entt::entity ent : entt::view all_pos = reg.view<pos>) {
//     auto nearby_ent_dir_x = reg.get<pos>(ent).pos_x - reg.get<pos>(entity).pos_x
//     auto nearby_ent_dir_y = reg.get<pos>(ent).pos_y - reg.get<pos>(entity).pos_y

//   }
// }

// #endif
