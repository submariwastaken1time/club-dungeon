#ifndef DRAW_ISLAND_INCLUDES
#define DRAW_ISLAND_INCLUDES
#include <cmath>
#endif

for angle = 0 to 360 degrees
  let r = 5 + sin(5 * angle_converted_to_radians)
  let x = r * cos(angle)
  let y - r * sin(angle)
